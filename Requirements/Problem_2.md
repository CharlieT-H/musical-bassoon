# ✅ Problem 2: Classic Configurable Rules

## 📋 Requirements
Define a list of rules, each consisting of:

An integer Divider  
A string Message


For each number in the range (default: 1 to 100):

* Apply all rules where number % Divider == 0.
* Concatenate all matching messages.
* If no rules match, print the number itself.

Add a new rule to the list:
* Divider: 7
* Message: "Bazz"
